# Shared Audio

Game-agnostic SFX and ambience shared across the Scourge universe.

Placeholder slots:

- `breach-collapse.ogg` — a breach (Scourge nest) collapsing once its repeater
  is severed.
- `choir-whisper.ogg` — the hive-mind ("the Choir") ambient whisper bed.

Drop real `.ogg` files here. Referenced from `assets-catalog.json` under
`shared` (`kind: "audio"`).
