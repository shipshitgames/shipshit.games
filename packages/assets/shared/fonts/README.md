# Shared Fonts

Game-agnostic typefaces shared across the Scourge universe, per `lore/DESIGN.md`.

Placeholder slots:

- `oswald.woff2` — Oswald, the display / heading face.
- `inter.woff2` — Inter, the body / UI face.

Subset and self-host the real font files here. Referenced from
`assets-catalog.json` under `shared` (`kind: "font"`).
