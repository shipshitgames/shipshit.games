# Shared UI

Game-agnostic UI icons and chrome shared across the Scourge universe. DOOM
canon: gunmetal + bone, blood/hellfire accents — never neon.

Placeholder slots:

- `icon-pyre.svg` — the Pyre faction mark (offense).
- `icon-warden.svg` — the Wardens faction mark (defense).
- Additional shared HUD/menu glyphs as needed.

These are referenced from `assets-catalog.json` under `shared` (`kind: "ui"`).
