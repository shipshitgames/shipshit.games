{
  "name": "@shipshitgames/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Ship Shit Games — Studio cockpit (Electron + Vite + React generator hub)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shipshitgames/shipshit.games.git",
    "directory": "apps/desktop"
  },
  "main": "dist/main/index.cjs",
  "type": "module",
  "scripts": {
    "dev": "node scripts/rebuild-native.cjs && vite",
    "build": "vite build",
    "package:mac": "node scripts/rebuild-native.cjs && bun run build && electron-builder --mac dmg --publish never",
    "preview": "vite preview",
    "start": "node scripts/rebuild-native.cjs && bun run build && NODE_ENV=production electron .",
    "postinstall": "node scripts/rebuild-native.cjs --soft",
    "rebuild:native": "node scripts/rebuild-native.cjs",
    "verify:terminal": "bun run build && node dist/verify/index.cjs",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.node.json",
    "deps:update": "bunx npm-check-updates -u --cooldown 3h --removeRange && bun install"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.1.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "build": {
    "appId": "games.shipshit.studio",
    "productName": "Ship Shit Games Studio",
    "files": [
      "dist/**/*",
      "!dist/verify/**",
      "package.json"
    ],
    "asarUnpack": [
      "**/node_modules/node-pty/**"
    ],
    "afterPack": "./scripts/after-pack.cjs",
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg"
      ]
    }
  },
  "devDependencies": {
    "@electron/rebuild": "4.0.3",
    "@types/node": "25.9.1",
    "@types/react": "19.2.16",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "electron": "42.3.3",
    "electron-builder": "26.15.0",
    "typescript": "6.0.3",
    "vite": "8.0.16",
    "vite-plugin-electron": "^1.0.4"
  }
}
